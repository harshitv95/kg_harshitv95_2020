# Kargo Engineering Online Assessment
> Python program to check if a one-to-one mapping exists in a string with another
Steps to execute:
```
python3 main.py abc bcd
```
You can replace 'abc' and 'bcd' with any input of your choice
